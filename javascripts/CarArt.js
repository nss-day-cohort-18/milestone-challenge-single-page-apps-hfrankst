"use strict";

var CarLot = (function (inventory) {
	//use this IIFE to build the car cards 
	for (inventory[0] in inventory) {
		var carCard = 
		`<article>
		<img>
		<p>Description</p>
		<p>Make</p>
		<p>Model</p>
		<p>Year</p>
		<p>Price</p>
		</article>`;
	}

	//build a function to change the border color and thickness


})(CarLot || {});